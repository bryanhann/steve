#!/bin/bash
export _0NAME=$(basename $0)
export _0PATH=$0
export _1NAME=${1-NULL}
export _1PATH=${_0PATH}.d/${_1NAME}
shift
[ ! -f ${_1PATH} ] && {
    echo "[$_0NAME]: missing '$_1NAME'" >&2
    exit 98
}
$_1PATH $*
